# ===============================================
# HIGH-VOLUME SCRAPER CONFIGURATION
# Target: 10 Lakh (1 Million) High-Quality Contacts
# ===============================================

database:
  host: localhost
  port: 5432
  name: scraper_db
  user: postgres
  password: your_password_here

proxy:
  # ===============================================
  # PROXY CONFIGURATION (Residential Support)
  # ===============================================
  host: "gw.dataimpulse.com:823"
  username: "1accade8fd4acb75b8ae"
  password: "c51e0887f7425452"

  enable_city_targeting: false
  
  proxies:
    - host: "gw.dataimpulse.com:823"
      username: "1accade8fd4acb75b8ae"
      password: "c51e0887f7425452"

scraper_settings:
  # Some sites (JustDial, TradeIndia) block HTTP/2 connections from known proxies.
  # Forcing HTTP/1.1 improves success rate on these targets.
  force_http1_sources: ["JUSTDIAL", "TRADEINDIA", "INDIAMART", "AMFI"]
  max_retries: 5
  timeout: 60
  request_delay_min: 5
  request_delay_max: 15
  max_concurrent: 5
  max_pages_per_source: 3

distributed:
  batch_size: 1000
  write_interval: 30
  task_queue: "scraper:tasks"
  result_buffer: "scraper:results_buffer"

cities:
  - "mumbai"
  - "delhi"
  - "bangalore"

categories:
  - "mutual-fund-agents"
  - "insurance-agents"
  - "chartered-accountants"
